{
  "name": "langchain-experiment",
  "license": "MIT",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "execute": "esbuild src/main.ts --bundle --platform=node --outfile=dist/main.js && dotenv -- node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^18.6.1",
    "concurrently": "^7.6.0",
    "dotenv-cli": "^7.1.0",
    "esbuild": "^0.14.50",
    "langchain": "^0.0.41",
    "typeorm": "^0.3.12",
    "typescript": "^5.0.0"
  }
}
